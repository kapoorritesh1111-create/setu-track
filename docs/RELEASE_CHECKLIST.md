**Baseline:** Phase 2.9 feature set (2.7 receipts + 2.8 export center + 2.9 client deliverables), branded for **SETU GROUPS / SETU TRACK**.  
**Last update:** 2026-03-05.

# Release Checklist (Current Baseline)

## ✅ Completed (verified in current repo)
### Pay cycles & projects
- [x] Project `pay_frequency` supports Weekly / Bi-weekly / Monthly (default)
- [x] Monthly cycles anchored to `week_start` (1st Monday → last Sunday style)

### Dashboard 2.1
- [x] Project selector present (role-appropriate)
- [x] KPIs scoped to selected project + current pay cycle
- [x] Cycle range label matches pay_frequency logic
- [x] Currency + hours formatting consistent

### Payroll 1.0
- [x] Payroll preview works (admin)
- [x] Close payroll creates payroll run snapshot
- [x] Locked period makes SETU TRACK + Approvals read-only
- [x] Payroll runs ledger loads and filters correctly

### UI/UX Modernization (Deel-aligned) — Phase 1.1 → 1.9
- [x] **StatusChip** system (consistent states)
- [x] **Metrics row** pattern (money-forward)
- [x] **PageHeader + SummaryBar** standard (hierarchy)
- [x] **TrustBlock** (audit context: refreshed/locked/locked_by)
- [x] **CommandCard** framing + **MetaFooter** (structured surfaces)
- [x] **ActionMenu** (exports + secondary actions)
- [x] **Global theme tokens** (theme/accent/density/radius)
- [x] **Theme-aware app background** (premium surface)
- [x] **DataTable styling** (sticky header, hover, spacing)
- [x] **Button hierarchy** (primary/secondary/ghost/danger)
- [x] **Primary page action** (top-right CTA, Phase 1.7)
- [x] **CommandBar** standard (Phase 1.8)
- [x] **Saved Views** scaffolding (Phase 1.9)
- [x] **Row-actions on hover** (cleaner tables)

### Approvals 1.0
- [x] Draft → Submit → Approve/Reject flow
- [x] Batch approve works (correct API payload)
- [x] Locked period disables actions and selection
- [x] Admin Unlock exists with payroll-run safety (409 + force confirmation)

### Payroll — Phase 2.0
- [x] Payroll run detail page renders from immutable snapshots (`payroll_run_*`)
- [x] Run exports available from run snapshot (summary + detail CSV)

### Payroll — Phase 2.2.1 (Hardened)
- [x] **Locked/Closed chips are green** (consistent “complete” state across Payroll + Payroll runs)
- [x] **Exports menu surface** is opaque + readable (no transparent dropdown)
- [x] **Locked by shows full name** (API returns `locked_by_name`, UI prefers name over UUID)
- [x] Payroll contractor picker includes **admin users** (admins can run/export their own reports if they have entries)
- [x] Contractor **PDF export view** remains print-ready and includes “Generated by” attribution

### Payroll — Phase 2.4 (Export receipts + audit history)
- [x] New `export_events` table (immutable receipts) with RLS (admin insert; admin/manager read)
- [x] Official export routes log best-effort receipts (CSV/PDF/Client ZIP)
- [x] Payroll report shows **Recent exports** for locked periods (period + optional project scope)
- [x] Payroll run detail shows **Export receipts** list (run-scoped)

### Payroll — Phase 2.5 (Diff model + narrative)
- [x] Snapshot-backed **diff vs previous locked period** (org + optional project scope)
- [x] Payroll report surfaces "Changes vs previous period" (top movers + totals)
- [x] Payroll run detail surfaces "Changes vs previous period" (run-scoped diff)
- [x] New API routes:
  - [x] `GET /api/payroll/run-diff?run_id=&project_id?`
  - [x] `GET /api/payroll/period-diff?period_start=&period_end=&project_id?`

### Build & quality gates
- [x] Vercel deployment succeeds
- [x] No TypeScript compile errors
- [x] No obvious console errors on key flows (Dashboard, Approvals, Payroll)

---

## 🔜 Required checks before every release
- [ ] `npm run build` passes locally
- [ ] Test locked/unlocked transition for a period (SETU TRACK + Approvals)
- [ ] Close payroll creates a run and locks the correct period
- [ ] If unlocking, ensure payroll-run safety warning triggers
- [ ] Verify role restrictions:
  - [ ] Contractor cannot approve/close/unlock/reopen
  - [ ] Manager can approve (if enabled) but cannot unlock/reopen unless explicitly allowed
  - [ ] Admin can close/unlock/reopen
- [ ] Mobile: dashboard and approvals usable without horizontal scroll


## Phase 2.6 — Export-to-export diff + Stage timeline
- [x] API: export-to-export variance (`/api/exports/since-last`) period + optional project scope
- [x] UI: Payroll report shows “Changes since last official export”
- [x] UI: Payroll run detail shows “Changes since last official export”
- [x] Stage timeline component (Preview → Approved → Locked → Exported → Paid)
- [x] DB: payroll_runs paid markers (paid_at/paid_by/paid_note) + RPC `mark_payroll_run_paid`
- [x] API: mark/unmark paid (`/api/payroll/runs/[id]/paid`)

## Phase 3.2 — Project exports paid lifecycle
- [x] DB: `project_exports` table with `is_paid/paid_by/paid_at/paid_note`
- [x] DB: link `export_events.project_export_id` (receipt → project export)
- [x] API: mark/unmark project export paid (`POST /api/projects/:projectId/exports/:exportId/paid`)
- [x] UI: Export receipt drawer shows project paid status + allows marking paid (admin)

## Phase 2.7 — Deel-level polish (Next)

Focus: interaction polish + guardrails so exports feel “client deliverable” grade.

- [ ] Export receipts drawer
  - [ ] Clicking a receipt row opens a drawer
  - [ ] Drawer shows: scope, filters, actor, run link, period, metadata
- [ ] “No changes since last export” badge + export guardrails
  - [ ] Header badge when locked + no_changes
  - [ ] Official export actions require confirm (“Export anyway”) when no_changes
- [ ] Paid confirmation + notes
  - [ ] Mark paid opens confirm modal/drawer
  - [ ] Optional paid note field stored
  - [ ] UI displays “Paid on … by …” + note
- [ ] Console error hygiene
  - [ ] No 404s on Payroll/Payroll Runs (common: `/api/payroll/period-diff`)
